# Copy to .env (local) and set the same vars in Vercel → Project → Settings →
# Environment Variables. PUBLIC_ prefix exposes them to the browser (required
# for client-side PostHog). Leave unset to disable analytics locally.
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com
